$(document).on('turbolinks:load', function() {
  // This used to set up jQuery UI autocomplete.
  // $('#completed_routine_category_name').autocomplete
  //   source: $('#completed_routine_category_name').data('autocomplete-source');
});
