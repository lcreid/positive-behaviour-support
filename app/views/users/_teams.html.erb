<ul class="list-group">
  <%= content_tag_for(:li, @user.subjects) do | patient | %>
  <%= link_to patient.short_name, person_path(patient), data: { ajax: false } %>
  <% end %>
  <li><%= link_to 'New Team', new_person_path, data: { ajax: false } %></li>
</ul>
